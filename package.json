{
  "name": "haex-claude-proxy",
  "version": "0.3.0",
  "description": "Generic Anthropic-API-compatible HTTP proxy that wraps `claude` CLI as a subprocess to use Claude Pro/Max OAuth tokens. Pluggable credential resolvers (builtins: file, token-map; external: any NPM module).",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  }
}
